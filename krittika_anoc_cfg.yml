# 3D basic-topology
topology: ['Switch']  # Ring, FullyConnected, Switch, HyperCube, Mesh, Bus

# Multiply each dim for total num NPUs
npus_count: [2]  # number of NPUs

# Bandwidth per each dimension
bandwidth: [200.0]  # GB/s

# Latency per each dimension
latency: [5.0]  # ns

# rows per each dimension
rows: [1]  

# cols per each dimension
cols: [2]  
